# PDF-chatbot
A Retrieval-Augmented Generation (RAG) chatbot that allows users to upload any PDF document and ask natural language questions about its content. The application processes the uploaded document, generates vector embeddings using Hugging Face, stores them in ChromaDB, retrieves the most relevant chunks, and uses Mistral AI to generate accurate,.
